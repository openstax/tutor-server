# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  settings:
    attributes:
      excluded_uids:
        name: 'Excluded exercise UIDs'
        help_block: 'Number@Version, comma-separated, list ALL versions that will be excluded'
      excluded_pool_uuid:
        name: 'Excluded exercise pool UUID'
        help_block: 'Set automatically when you update the list above'
      import_real_salesforce_courses:
        name: 'Import real Salesforce courses?'
        help_block: 'Unchecked: limits to Denver University'
      term_years_to_import:
        name: 'Term years to import'
        help_block: 'Comma-separated list of Salesforce-formatted term years, e.g. "2015 - 16 Spring, 2016 - 17 Fall", blank to disable import'
      default_open_time:
        name: 'Default task open time'
        help_block: 'In the format HH:MM'
      biglearn_client:
        name: 'Biglearn Client'
        labels:
          local_query_with_real: 'local_query_with_real'
          local_query_with_fake: 'local_query_with_fake'
          real: 'real'
          fake: 'fake'
        help_block: 'real is full Biglearn; local_query is real or fake BL for writes and local for queries'
      default_due_time:
        name: 'Default task due time'
        help_block: 'In the format HH:MM'
      course_appearance_codes:
        name: 'Valid Course Appearance Codes'
        help_blocK: 'As a Ruby Hash'
