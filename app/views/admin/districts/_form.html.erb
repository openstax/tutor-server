<% method ||= :post %>

<%= lev_form_for :district, url: url, method: method do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>
