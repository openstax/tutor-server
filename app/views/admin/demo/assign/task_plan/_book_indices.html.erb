<%
  # Clients of this partial must provide the following variable:
  # form
  # Additionally, they may override the following variables:
  options ||= {}
%>

<div class="form-group">
  <%= form.label :unit %>
  <%=
    form.number_field :unit, options.merge(
      class: 'form-control',
      placeholder: '0-based',
      min: 0
    )
  %>
</div>

<div class="form-group">
  <%= form.label :chapter %>
  <%=
    form.number_field :chapter, options.merge(
      class: 'form-control',
      placeholder: '0-based',
      min: 0
    )
  %>
</div>

<div class="form-group">
  <%= form.label :section %>
  <%=
    form.number_field :section, options.merge(
      class: 'form-control',
      required: true,
      placeholder: '0-based and Required (unless Book location is removed)',
      min: 0
    )
  %>
</div>
