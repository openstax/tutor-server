<!DOCTYPE html>
<html>
  <head>
    <title>OpenStax Tutor</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <%= OpenStax::Utilities::Assets.tags_for('lmspair') %>
    <%= csrf_meta_tags %>
    <%= render 'webview/google_analytics' %>
  </head>
  <body>
    <div id="ox-root-view">
      TODO: tutor-server app/views/lti/pair.html.erb (tutor-js tutor/src/screens/lms-pair/)

      <%= button_to 'Pair any course I have', lti_pair_url(
        course_id: current_user.roles.map(&:teacher).compact.reject(&:deleted?).first.course.id
      ) %>
    </div>
  </body>
</html>
