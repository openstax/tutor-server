<% @page_header = 'Ecosystems' %>

<%= render partial: 'manager/ecosystems/index',
           locals: { ecosystems: @ecosystems,
                     incomplete_jobs: @incomplete_jobs,
                     failed_jobs: @failed_jobs,
                     import_path: import_content_analyst_ecosystems_path,
                     destroy_path: Proc.new { |*args| content_analyst_ecosystem_path(args) } } %>
